# Core Wyze integration — handles TUTK P2P connection and auth
wyzecam>=0.4.4

# TOTP 2FA support for accounts with two-factor authentication
pyotp>=2.9.0

# Used by wyzecam for Wyze REST API calls (pinned for predictable behaviour)
requests>=2.31.0,<3

# YAML parsing (used to validate mediamtx config at startup if needed)
pyyaml>=6.0.1
